= sinatra-static

export your sinatra app to static files - get requests only, currently no redirects allowed


== usage

  require 'sinatra_static'

  builder = SinatraStatic.new(MySinatraApp)
  builder.build!('/Users/paul/my_static_site')
 
