= sinatra-static

export your sinatra app to static files - get requests and status 200 only (no redirects)


== usage

  require 'sinatra_static'

  builder = SinatraStatic.new(MySinatraApp)
  builder.build!('/Users/paul/my_static_site')
 
